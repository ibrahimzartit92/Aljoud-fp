Aljoud Branch Agent (Windows)

- Install Python 3.11+
- pip install -r requirements.txt
- copy config.example.json config.json and edit
- run: python agent.py config.json

Endpoints:
- POST /sync/users (server -> agent)
- GET /health
